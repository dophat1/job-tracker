# Track your job applications

## Project structure

```bash
job-tracker/
├── db/
│   ├── database.py   # connection + schema
│   └── queries.py    # CRUD functions
├── pages/
│     ├──         # (empty for now — Streamlit auto-detects this folder)
├── utils/             # (empty for now — chart/metric helpers go here later)
├── data/               # SQLite file lives here
└── requirements.txt
```
